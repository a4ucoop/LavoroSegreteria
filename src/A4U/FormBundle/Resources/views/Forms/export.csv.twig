{% if kind == "PAE" or kind == "PAI" %}
Nome	Cognome	Indirizzo	CAP	Città	Email	Tel.	"Data di nascita"	"Luogo di nascita"	"Scuola di provenienza"	"Partecipato altre attività"	Attività	"Altra attività"	Referenze	"Altra referenza"	"Corso di laurea"	"Codice Fiscale"	"Data di visita"
    {% for row in data %}
        "{{row.getName}}"	"{{row.getSurname}}"	"{{row.getAddress}}"	"{{row.getCap}}"	"{{row.getCity}}"	"{{row.getEmail}}"	"{{row.getPhone}}"	"{{row.getBirthDateAsString}}"	"{{row.getBirthPlace}}"	"{{row.getAttendedSchool}}"	"{{row.getHasAttendedToOtherActivitiesAsString}}"	"{{row.getActivity}}"	"{{row.getOtherActivity}}"	"{{row.getReference}}"	"{{row.getOtherReference}}"	"{{row.getUnicamCourse}}"	"{{row.getFiscalcode}}"	"{{row.getReservationDateAsString}}"
    {% endfor %} 
{% elseif kind == "PAEreport" %}
Nome	Cognome "Codice Fiscale"	Immatricolato	"Corso di laurea (form)"	"Corso di laurea (ESSE3)"	ANNO	CFU	MEDIA	Indirizzo	CAP	Città	Email	Tel.	"Data di nascita"	"Luogo di nascita"	"Scuola di provenienza"	"Partecipato altre attività"	Attività	"Altra attività"	Referenze	"Altra referenza"   "Data di visita"
    {% for row in data %}
        "{{nome}}"	"{{cognome}}"    "{{cfstudente}}"    "{{immatricolato}}"    "{{nome_cds}}"  "{{anno}}"  "{{cfu}}"   "{{media}}"  "{{row.getUnicamCourse}}"   "{{row.getAddress}}"	"{{row.getCap}}"	"{{row.getCity}}"	"{{row.getEmail}}"	"{{row.getPhone}}"	"{{row.getBirthDateAsString}}"	"{{row.getBirthPlace}}"	"{{row.getAttendedSchool}}"	"{{row.getHasAttendedToOtherActivitiesAsString}}"	"{{row.getActivity}}"	"{{row.getOtherActivity}}"	"{{row.getReference}}"	"{{row.getOtherReference}}"	"{{row.getReservationDateAsString}}"
    {% endfor %} 
{% elseif kind == "OpenDay" %}
Nome	Cognome	Indirizzo	CAP	Città	Email	Tel.	"Data di nascita"	"Luogo di nascita"	Corso	"Attività frequentata"	"Codice Fiscale"
    {% for row in data %}
        "{{row.getName}}"	"{{row.getSurname}}"	"{{row.getAddress}}"	"{{row.getCap}}"	"{{row.getCity}}"	"{{row.getEmail}}"	"{{row.getPhone}}"	"{{row.getBirthDateAsString}}"	"{{row.getBirthPlace}}"	"{{row.getAttendedSchool}}"	"{{row.getAttendedActivity}}"	"{{row.getFiscalcode}}"
    {% endfor %} 
{% endif %}
